{
  "name": "@web3uoa/glyde",
  "version": "0.0.3",
  "description": "Express middleware that paywalls API endpoints using x402 — agents pay per request in USDC on Avalanche",
  "keywords": ["x402", "paywall", "express", "middleware", "avalanche", "usdc", "ai-agents", "eip-712", "eip-3009"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Igordro1d/web3_hackathon"
  },
  "files": ["dist", "README.md"],
  "publishConfig": { "access": "public" },
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.0",
    "viem": "^2.21.0"
  },
  "peerDependencies": {
    "express": ">=4"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@web3nz/shared": "workspace:*"
  }
}
