# Avalanche Fuji testnet
RPC_URL=https://api.avax-test.network/ext/bc/C/rpc
CHAIN_ID=43113

# Sepolia RPC used by the dashboard for ENS reverse lookup
VITE_SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# ----------------------------------------------------------------------------
# Supabase — backs accounts, products, and transactions
# ----------------------------------------------------------------------------
# Project URL (Settings → API in the Supabase dashboard)
SUPABASE_URL=https://nhypczhfaixwwxeupsgm.supabase.co

# Anon / publishable key — safe to use in browser bundles, used here only on
# the server to verify JWTs and run the auth.signInWithPassword flow.
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5oeXBjemhmYWl4d3d4ZXVwc2dtIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzcxNTc2NTksImV4cCI6MjA5MjczMzY1OX0.Ulc5cQw4VChFinCA0rrwX4Ev6RO1oeOvF-S6s809o9g

# Service role key — bypasses RLS. Server-only. NEVER ship to a browser.
# Get it from Supabase dashboard → Settings → API → service_role (Project API keys).
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# ----------------------------------------------------------------------------
# Paywall middleware
# ----------------------------------------------------------------------------
# Wallet for paywall settlement (DO NOT commit real keys)
PAYWALL_PRIVATE_KEY=0x...

# Dashboard backend used by paywall middleware for product config
DASHBOARD_BACKEND_URL=http://localhost:3001

# Product API key copied from the merchant dashboard
PRODUCT_API_KEY=pk_live_...

# Product config cache duration in middleware
PRODUCT_CONFIG_CACHE_TTL_MS=30000

# USDC on Fuji testnet
USDC_CONTRACT_ADDRESS=0x5425890298aed601595a70AB815c96711a31Bc65

# ----------------------------------------------------------------------------
# Agent
# ----------------------------------------------------------------------------
# Agent wallet (signs EIP-712 authorizations — does NOT need AVAX for gas)
AGENT_PRIVATE_KEY=0x...

# Anthropic API key for the Claude AI demo agent
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI key used by the agent chat backend
OPENAI_API_KEY=sk-...
